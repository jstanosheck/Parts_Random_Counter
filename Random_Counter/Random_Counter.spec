# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['Random_Counter.py'],
             pathex=['/Users/jacobstanosheck/Documents/GitHub/Parts_Random_Counter/Random_Counter'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='Random_Counter',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=False )
app = BUNDLE(exe,
             name='Random_Counter.app',
             icon=None,
             bundle_identifier=None)
